<p>These are <a href="#"><%= @contacts.length %></a> contacts</p>

<div>
  <% @contacts.each do |contact| %>
    <section> <%= contact.full_name %> ( <%= contact.email %> )
      <div class="link">
        <a href="">Edit</a> |
        <a href="">Delete</a>
      </div>

    </section>
  <% end %>
</div>
<a href="http://localhost:4567/home"></a>
